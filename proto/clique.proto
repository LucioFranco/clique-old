syntax = "proto3";

package clique.proto;

service Member {
  rpc join(JoinRequest) returns (Peer);
}

message JoinRequest {
  repeated string peers = 2;
}

message Peer {
  repeated string peers = 2;
}